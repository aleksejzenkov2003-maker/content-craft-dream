

## Выровнять размеры фото в диалоге духовника

Сейчас «Главное фото» имеет ширину `w-48` (192px), а «Миниатюра» — `w-36` (144px). Нужно сделать их одинаковыми.

### Изменение

В `src/components/advisors/AdvisorsGrid.tsx`:
- Строка 291: изменить `w-36` → `w-48` для контейнера миниатюры

Одна строка, оба фото станут одинакового размера `w-48` с пропорцией `9:16`.

