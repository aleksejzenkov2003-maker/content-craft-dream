
Цель: убрать зависание на этапе `Субтитры 14%` и сделать процесс предсказуемым (с понятными этапами, таймаутами, отменой и без “вечного” ожидания).

1) Усилить загрузчик FFmpeg (`src/lib/ffmpegLoader.ts`)
- Разделить процесс на этапы и прогресс:
  - загрузка `ffmpeg-core.js`,
  - загрузка `ffmpeg-core.wasm`,
  - инициализация/компиляция ядра.
- Для этапа компиляции (где сейчас “залипает” на 14%) добавить отдельный “heartbeat” прогресс (например 14→20), чтобы UI не выглядел зависшим.
- Добавить таймауты и `AbortController` для сетевых запросов, плюс retry по CDN.
- После любой ошибки гарантированно:
  - сбрасывать `loadingPromise`,
  - очищать объект FFmpeg,
  - освобождать `blob:` URL (revoke), чтобы не копить память.

2) Сделать процесс вшивки “управляемым” (`src/lib/videoSubtitles.ts`, `src/components/videos/VideoSidePanel.tsx`)
- Добавить поэтапные статусы в UI (не только %):
  - “Загрузка FFmpeg”
  - “Инициализация FFmpeg”
  - “Скачивание видео”
  - “Вшивка субтитров”
  - “Загрузка результата”
- Добавить кнопку “Отменить”, чтобы пользователь мог прервать зависшую операцию.
- Протащить `AbortSignal` в `burnSubtitles` и прерывать долгие fetch/exec с понятной ошибкой.
- Добавить общий watchdog-таймаут на весь процесс вшивки (например 6–8 минут) с корректным завершением состояния в UI.

3) Убрать источники долгих “подвисаний” и утечек
- В `videoSubtitles.ts` использовать уникальные временные имена файлов в виртуальной ФС FFmpeg (чтобы избежать коллизий при повторных запусках).
- Гарантированно снимать `progress` listener и чистить временные файлы в `finally`.
- В `videoNormalizer.ts`:
  - передавать прогресс в `getSharedFFmpeg(...)`,
  - снимать `progress` listener после завершения (сейчас есть риск накопления обработчиков и деградации производительности).

4) Снизить вероятность “первого зависания” за счёт прогрева
- Ленивая предзагрузка FFmpeg в фоне при открытии панели видео (или при появлении `word_timestamps`), чтобы клик “Вшить субтитры” не стартовал с нуля.
- Прогрев делать безопасно: без блокировки UI, с тихим fail (если сеть плохая — не ломаем основной сценарий).

5) Проверка после внедрения
- Кейсы:
  - нормальная сеть: прогресс идёт по этапам, файл загружается, статус сбрасывается.
  - медленная сеть: нет “вечных” 14%, показывается этап и остаётся управление.
  - отмена: операция останавливается, кнопка снова активна, нет зависших состояний.
  - повторный запуск сразу после отмены/ошибки: работает корректно.
- Дополнительно проверить, что в других местах с FFmpeg (нормализация) нет регрессии по производительности.

Технические детали (что именно менять):
- `src/lib/ffmpegLoader.ts`:
  - таймауты + abort + retry,
  - прогресс этапа компиляции,
  - корректный cleanup (`loadingPromise`, ffmpeg instance, blob URLs).
- `src/lib/videoSubtitles.ts`:
  - поддержка `AbortSignal`,
  - этапные callbacks для UI,
  - уникальные temp filenames,
  - надёжный cleanup.
- `src/components/videos/VideoSidePanel.tsx`:
  - расширить состояние subtitle-процесса (phase + progress + cancellable),
  - добавить кнопку отмены,
  - улучшить тексты статуса.
- `src/lib/videoNormalizer.ts`:
  - cleanup listener’ов,
  - прогресс инициализации FFmpeg.
