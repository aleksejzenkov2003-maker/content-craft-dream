
# Масштабное обновление разделов «Вопросы» и «Ролики»

## Раздел 1: ВОПРОСЫ

### 1.1 Обновление значений полей

**Безопасность** - новые варианты:
- `safe` -> "Безопасно" (оставить)
- `critical` -> "Критический" (новый)
- `medium_risk` -> "Средний риск" (новый)
- `high_risk` -> "Высокий риск" (новый)
- Убрать старые "Внимание", "Опасно", "Не проверено"

**Актуальность** - числовое поле от 1 до 100 (уже числовое, но нужно убрать отображение "---" и показывать число)

**Статус** - новые варианты (выпадающее меню):
- `not_selected` -> "Не отобран"
- `in_progress` -> "Взят в работу"
- `published` -> "Опубликован"
- Убрать старые "Ожидает", "Проверен"

**Дата** -> переименовать заголовок в "Плановая публикация"

### 1.2 Удаление элементов UI

| Элемент | Файл | Действие |
|---------|------|----------|
| Чекбокс рядом со Статусом (фильтр-чекбокс) | QuestionsTable.tsx (строка 709-713) | Удалить |
| Кнопка "Добавить" | QuestionsTable.tsx (строка 499-502) | Удалить |
| Надпись "Interface/Questions" | QuestionsTable.tsx (строка 464-468) | Удалить |
| Кнопки "Group", "Sort" | QuestionsTable.tsx (строка 470-477) | Удалить |
| Строка поиск/запуск/обновление в Header | Header.tsx (строки 28-64) | Удалить |
| Панель "выбрано для фильтра" | QuestionsTable.tsx (строки 563-579) | Удалить |
| Боковая панель редактирования при клике | QuestionsTable.tsx | Убрать вызов handleRowClick |
| Toast "Вопрос обновлен" | Index.tsx (строка 391) | Убрать toast |

### 1.3 Перенос кнопки "Импорт"

- Убрать кнопку "Импорт CSV" из action bar (строка 495-498)
- Разместить её в верхней панели QuestionsTable (рядом со счетчиком записей)
- Поддержка Excel (.xlsx) уже есть через библиотеку `xlsx`

### 1.4 Сортировка

Добавить сортировку для полей:
- Безопасность (по алфавиту значений)
- Актуальность (числовая)
- Вопрос (по алфавиту)
- Статус (по алфавиту значений)

Сортировка по ID и Дате уже реализована.

### 1.5 HoverCard для вопроса

- Сделать шире (с `w-80` на `w-[500px]`)
- Добавить более тёмный фон (`bg-popover/95` или `bg-slate-900` для тёмной темы)

### 1.6 Bulk actions - изменить набор кнопок

Оставить только (в указанном порядке):
1. Плановая дата публикации
2. Статус
3. Удалить

Убрать: Безопасность, Обложки

### 1.7 Обновление фильтров (QuestionFilters.tsx)

- Убрать фильтр "Наличие роликов"
- Обновить статусы и безопасность под новые значения
- Добавить фильтр безопасности "от ___ до ___" (числовой диапазон) -- для этого нужен числовой safety_score, но текущее поле текстовое. Реализуем как фильтр по категориям безопасности (чекбоксы), так как поле safety_score хранит строковые значения.

### 1.8 Без перезагрузки при изменении

- При изменении статуса или даты -- не показывать toast "Вопрос обновлен"
- Не вызывать полную перезагрузку (использовать `silent: true` и `skipRefetch` где возможно)

### 1.9 Автоматические действия (автогенерация)

Когда заполнены оба поля "Плановая публикация" и статус "Взят в работу":
- Автоматически запустить генерацию передних обложек для всех связанных роликов
- Автоматически запустить генерацию видео (аудио + HeyGen видео)
- Проверка: не генерировать повторно если cover_status === 'ready' или generation_status === 'ready'

Файл: `Index.tsx` -> `onUpdateQuestion` -- добавить проверку условий и вызов генерации

---

## Раздел 2: РОЛИКИ

### 2.1 Верхнее меню - убрать элементы

| Элемент | Действие |
|---------|----------|
| Строка поиска | Удалить |
| Селект духовников | Удалить |
| Селект плейлистов | Удалить |
| Кнопка "Новый ролик" | Удалить |

### 2.2 Фильтрация по статусу "Взят в работу"

Показывать только видео, у которых `question_status === 'in_progress'` (вместо текущего `checked`/`approved`)

### 2.3 Формат заголовка группы

Изменить с текущего `#{questionId} | questionText` на:
`#{questionId}, {questionText}, {planned_date}`

### 2.4 Убрать редактируемые поля в строке

Заменить InlineEdit для cover_status, video_status и duration на простое отображение текста.

### 2.5 Просмотр по клику на ID

- Клик на строку больше не открывает side panel
- Только клик на ID ролика (video_number) открывает модальное окно

### 2.6 Модальное окно (VideoSidePanel)

- Добавить текстовое поле "Ответ духовника" (`advisor_answer`)
- Сделать Publication date редактируемым (DatePicker)
- По умолчанию первая обложка уже сгенерирована при попадании в раздел
- Generate Cover и Generate Video -- кнопки для повторной генерации
- Убрать toast "Ролик обновлен"
- Видео-плеер сделать таким же по размеру как обложка
- Кнопка Generate Video -- генерация в HeyGen без обложек
- Каналы по умолчанию -- из настроек духовника (пока нет поля, используем все активные)
- Каналы для публикации переместить на самый верх панели

### 2.7 Кнопка "Публикация"

При нажатии -- создаёт задачи публикации по каждому выбранному каналу (без автоматической публикации). По каждой публикации автоматически генерируется текст описания через LLM (уже реализовано в `usePublications.addPublication`).

---

## Технические файлы для изменения

| Файл | Изменения |
|------|-----------|
| `src/components/questions/QuestionsTable.tsx` | Новые safety/status опции, удаление UI элементов, сортировка, HoverCard, bulk actions, автогенерация |
| `src/components/questions/QuestionFilters.tsx` | Новые статусы/безопасность, убрать фильтр роликов |
| `src/components/videos/VideosTable.tsx` | Убрать верхние фильтры, формат заголовка, убрать inline edit, клик на ID |
| `src/components/videos/VideoSidePanel.tsx` | Ответ духовника, дата, порядок элементов, размер видео |
| `src/pages/Index.tsx` | Убрать toast, автогенерация при смене статуса, обновление filter logic |
| `src/components/layout/Header.tsx` | Убрать поиск/запуск/обновление |
